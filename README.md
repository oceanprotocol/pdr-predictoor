First cut: WIP, nothing works


Start barge:
```bash
export CONTRACTS_VERSION=predictoor
export SUBGRAPH_VERSION=predictoor
./start_ocean.sh --predictoor
```

- reads from subgraph list of template3 contracts
- 